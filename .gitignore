# Dependencies
node_modules/

# Build outputs
dist/
.output/
.vite/

# TanStack / Vite generated
.tanstack/
.content-collections/

# Drizzle migration metadata (generated artifacts)
drizzle/meta/

# Environment (.env files are committed — local dev values only, no prod secrets)
# Prod uses Doppler — no .env files at runtime
.env.local
*.local

# RALPH runner state and logs
.ralph-tasks.json
.ralph-logs/

# Bun creates a literal "~" directory in the project root for its install cache symlinks
~/

# OS / Editor
.DS_Store
*.swp
*.swo

# Misc
AGENTS.md
.vite-hooks/

# NOTE: bun.lock IS committed (Bun's lockfile)
